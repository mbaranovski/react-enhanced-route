version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Build
          command: npm run build
      - run:
          name: Test
          command: npm run test
